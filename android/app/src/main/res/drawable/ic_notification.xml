<?xml version="1.0" encoding="utf-8"?>
<!-- Status-bar icons are rendered as a single-colour mask, so this is the send
     mark as one solid silhouette rather than the two-tone launcher glyph. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M3,12 L21,4 L14,20 L11.5,14.5 Z" />
</vector>
